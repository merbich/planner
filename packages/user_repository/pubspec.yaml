name: user_repository
description: Dart package which manage the user.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.1.3 <4.0.0'

dependencies:

  flutter:
    sdk: flutter
  equatable: ^2.0.5
  firebase_auth: ^4.16.0
  cloud_firestore: ^4.15.3
  flutter_bloc: ^8.1.3

dev_dependencies:
  flutter_lints: ^3.0.1
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

